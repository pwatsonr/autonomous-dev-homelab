# Implementation Plan: REQ-000055 — Live Homelab End-to-End Enablement

| Field | Value |
|---|---|
| **Title** | Live Homelab End-to-End Enablement — Build, Config, Vault, Live Loop, Deploy |
| **Request** | REQ-000055 |
| **Parent TDD** | `docs/tdd/REQ-000055-req-000055.md` |
| **Parent PRD** | `docs/prd/REQ-000055-req-000055.md` |
| **Author** | Plan Author Agent (autonomous-dev) |
| **Date** | 2026-07-01 |
| **Version** | 1.0 |
| **Status** | Draft — pending plan review |

---

## 1. Plan Metadata

- **Total estimated effort**: **47.4 h** (39.5 base + 20% buffer).
- **Critical path**: T-01 → T-02 → T-04 → T-05 → T-06 → T-07 → T-08 → T-09 → T-10 → T-14 → T-16 (14 tasks, **~30.6 h** buffered).
- **Parallel tracks**: 4 (build/packaging, core wiring, deploy, evidence/runbook).
- **Sizing**: All tasks 0.5–4 h per plan-author constraint. No task larger than 4 h; long items already split.
- **Design coverage**: Every FR-* / ADR / section in TDD §3–§7 and §10–§14 maps to at least one task (§6 of this plan).

---

## 2. Task List

### TASK-001 — Fix `TS2493` in safety CLI test (ADR-01)

- **ID**: TASK-001 (≡ TDD T-01)
- **Description**: Repair the `tsc --noEmit` failure at `tests/cli/test-safety-cli.test.ts:133` by typing the audit mock as `jest.MockedFunction<CancelAuditFn>` per TDD §3.1 and ADR-01. **No** changes to `tsconfig.json`. Also update any sibling assertions in the same file that rely on the same untyped mock (search for `audit.mock.calls`).
- **Files**:
  - Modify: `tests/cli/test-safety-cli.test.ts`
  - Verify import: `src/cli/commands/cancel-action.ts` (exports `CancelAuditFn`; add export if missing)
- **Dependencies**: none.
- **Acceptance criteria**:
  1. `npx tsc --noEmit` exits 0.
  2. `npx jest tests/cli/test-safety-cli.test.ts` exits 0 with all cases still green.
  3. No other production or test file is modified beyond the mock typing (and, if needed, a one-line named export in `cancel-action.ts`).
  4. `git diff --stat` shows ≤2 files touched.
- **Lint/Test commands**: `npm run lint`, `npx tsc --noEmit`, `npx jest tests/cli/test-safety-cli.test.ts`.
- **Effort**: 0.5 h base → **0.6 h buffered**.
- **Track**: A (Build/Typecheck).
- **Risks**: `CancelAuditFn` may not currently be exported → low; mitigation: add a `export type CancelAuditFn` alongside existing signature.

### TASK-002 — Build config, npm scripts, and clean-build pipeline (FR-BLD-02/03)

- **ID**: TASK-002 (≡ TDD T-02)
- **Description**: Introduce `tsconfig.build.json`, `scripts/build.sh`, `scripts/secret-scan.sh`, and the `test`/`test:coverage`/`typecheck`/`lint`/`build`/`build:all`/`package` npm scripts per TDD §3.2. `secret-scan.sh` greps `dist/` for pattern list (`BEGIN * PRIVATE KEY`, `password`, `secret`, `token`, `AKIA[0-9A-Z]{16}`, `xoxb-`, etc.) and exits non-zero on hit.
- **Files**:
  - Create: `tsconfig.build.json`, `scripts/build.sh`, `scripts/secret-scan.sh`
  - Modify: `package.json` (scripts block only; no dep changes here)
- **Dependencies**: TASK-001.
- **Acceptance criteria**:
  1. On a fresh clone, `bash scripts/build.sh` exits 0 in ≤120 s wall-clock (NFR-01).
  2. `node dist/cli/index.js --help` prints top-level command list.
  3. `dist/` contains compiled JS + `.d.ts` for every `src/**/*.ts` file; no `tests/` output.
  4. `scripts/secret-scan.sh dist/` exits 0 on clean checkout; exits non-zero when a fixture secret is planted.
  5. `chmod +x` on `dist/cli/index.js` after build.
- **Lint/Test commands**: `bash scripts/build.sh`, `bash scripts/secret-scan.sh dist/`, `node dist/cli/index.js --help`.
- **Effort**: 2 h base → **2.4 h buffered**.
- **Track**: A (Build/Typecheck).
- **Risks**:
  - `M` — Existing `src/` may reference test-only helpers; mitigation: run `tsc -p tsconfig.build.json` first, then move any leaks into `tests/`.
  - `L` — `npm ci` cold on the CI runner may exceed 120 s; mitigation: exclude `npm ci` from the timing target (it is one-off) and time only steps after install.

### TASK-003 — Packaging: `scripts/package.sh` + Claude plugin manifest validation (FR-BLD-04, ADR-05)

- **ID**: TASK-003 (≡ TDD T-03)
- **Description**: Add `scripts/package.sh` producing `dist/autonomous-dev-homelab-<version>.tgz` per TDD §3.3. Validate `.claude-plugin/plugin.json` against Claude Code plugin schema (JSON-schema validate against local `schemas/plugin-manifest.schema.json` if present; else structural check for required fields `name`, `version`, `entrypoints`).
- **Files**:
  - Create: `scripts/package.sh`
  - Optional create: `schemas/plugin-manifest.schema.json` (only if not already shipped)
  - Modify: `package.json` (`files` array to control tarball contents)
- **Dependencies**: TASK-002.
- **Acceptance criteria**:
  1. `bash scripts/package.sh` produces `dist/autonomous-dev-homelab-<version>.tgz`.
  2. `tar -tzf` on the tarball shows exactly: `.claude-plugin/plugin.json`, `dist/**`, `schemas/**`, `dashboards/homelab.json`, `package.json`, `README.md`, `LICENSE`.
  3. Tarball's `package.json` contains no `devDependencies`.
  4. Manifest validation exits non-zero when a required field is stripped (verified via fixture).
- **Lint/Test commands**: `bash scripts/package.sh`, `tar -tzf dist/autonomous-dev-homelab-*.tgz`.
- **Effort**: 2 h base → **2.4 h buffered**.
- **Track**: A (Build/Typecheck).
- **Risks**: `L` — README/LICENSE may not yet exist; mitigation: add minimal stubs referenced by PRD.

### TASK-004 — Config schema, loader, types + tests (FR-CFG-01/02/03)

- **ID**: TASK-004 (≡ TDD T-04)
- **Description**: Implement Zod schema + loader per TDD §3.4 with `.strict()`, `CredentialRef`, `Host`, `HomelabConfig`, `assertNoInlineSecrets` deep-walk guard, and `formatZodError` producing actionable path-scoped messages.
- **Files**:
  - Create: `src/config/schema.ts`, `src/config/loader.ts`, `src/config/types.ts`
  - Create tests: `tests/config/schema.test.ts`, `tests/config/loader.test.ts`
  - Create fixtures: `tests/config/fixtures/valid.yaml`, `invalid-inline-secret.yaml`, `invalid-missing-vault.yaml`, `invalid-bad-cidr.yaml`
- **Dependencies**: TASK-001.
- **Acceptance criteria**:
  1. Valid fixture parses to fully-typed `HomelabConfig`.
  2. Every invalid fixture rejected with error containing (a) the offending JSON-path and (b) the failed constraint text.
  3. Inline `password`/`key`/`secret`/`token`/`value` fields anywhere in tree → `ConfigInvalidError` (exit 11).
  4. Unknown YAML keys rejected by `.strict()`.
  5. Coverage ≥90% on `src/config/`.
- **Lint/Test commands**: `npm run lint`, `npx jest tests/config`.
- **Effort**: 3 h base → **3.6 h buffered**.
- **Track**: B (Core wiring).
- **Risks**: `L` — YAML anchors could bypass strict; mitigation: reject any parsed value whose `constructor.name` differs from expected primitive/object.

### TASK-005a — Secret resolver types + Vault HTTP client (FR-SEC-01..04, ADR-02)

- **ID**: TASK-005a (split of TDD T-05, part 1)
- **Description**: Implement `src/secrets/types.ts` (`CredentialRef`, `ResolvedSecret`, `SecretResolver`) and a thin fetch-based Vault client (~150 LOC) with AppRole login (`POST /v1/auth/approle/login`), token caching (respect `auth.lease_duration`), and kv v2 read (`GET /v1/{kv}/data/{path}`). Include `refHash` computation (SHA-256 of `vault:{path}:{field}:{version}`).
- **Files**:
  - Create: `src/secrets/types.ts`, `src/secrets/vault-resolver.ts`
  - Create tests: `tests/secrets/vault-resolver.test.ts` (using Node `undici` mock or `nock`)
- **Dependencies**: TASK-004.
- **Acceptance criteria**:
  1. Happy path: `resolve({vault_path, vault_field})` returns `ResolvedSecret` with correct `value: Buffer`, deterministic `refHash`.
  2. `ping()` returns void on 200/429/503-with-standby; throws `VaultUnreachableError` on connect refuse.
  3. AppRole login errors → `VaultAuthError`; 403 on kv read → `VaultPermissionError`; missing field → `SecretMissingError`.
  4. Token cached across calls until 90% of TTL elapsed (verified by mock counting login hits).
- **Lint/Test commands**: `npm run lint`, `npx jest tests/secrets/vault-resolver.test.ts`.
- **Effort**: 3 h base → **3.6 h buffered**.
- **Track**: B (Core wiring).
- **Risks**:
  - `M` — Vault kv v1 vs v2 path shape divergence; mitigation: derive path from configured `vault_path` verbatim (config declares full mount path).
  - `L` — Node `fetch` timeout semantics; mitigation: use `AbortController` with 5 s default.

### TASK-005b — Redactor + secret error taxonomy (FR-SEC-04)

- **ID**: TASK-005b (split of TDD T-05, part 2)
- **Description**: Implement `src/secrets/redactor.ts` audit-sink filter and `src/secrets/errors.ts` typed error classes per TDD §3.5 error table.
- **Files**:
  - Create: `src/secrets/redactor.ts`, `src/secrets/errors.ts`
  - Create tests: `tests/secrets/redactor.test.ts`, `tests/secrets/errors.test.ts`
  - Modify: existing `AuditWriter` (attach redactor as sink filter; leave writer API untouched)
- **Dependencies**: TASK-005a.
- **Acceptance criteria**:
  1. Redactor rejects events containing any of {`value`, `secret`, `token`, `key`, `password`} as an unbased field name; emits `SECRET_LEAK_DETECTED` marker containing only the field name.
  2. Whitelist per event type enforced (documented as a data table).
  3. Every error class has `code`, `exit`, and stable user-facing message string exactly matching TDD §6 table.
  4. Coverage ≥90% on `src/secrets/`.
- **Lint/Test commands**: `npm run lint`, `npx jest tests/secrets`.
- **Effort**: 2 h base → **2.4 h buffered**.
- **Track**: B (Core wiring).
- **Risks**: `M` — Existing `AuditWriter` may not expose a sink-filter hook; mitigation: introduce an internal `WriteInterceptor` chain (documented as private API); TASK-005b acceptance verifies zero behavior change on non-secret events.

### TASK-006 — Transport-select + runtime bootstrap (FR-CONN-01, ADR-06)

- **ID**: TASK-006 (≡ TDD T-06)
- **Description**: Implement `src/live/transport-select.ts` (`pickTransport`) and `src/live/bootstrap.ts` (`assembleRuntime`) per TDD §3.6 and §2.3(1). `assembleRuntime` loads config, constructs `VaultSecretResolver`, pings vault (fail-closed), returns `Runtime`.
- **Files**:
  - Create: `src/live/transport-select.ts`, `src/live/bootstrap.ts`
  - Create tests: `tests/live/transport-select.test.ts`, `tests/live/bootstrap.test.ts`
- **Dependencies**: TASK-004, TASK-005a.
- **Acceptance criteria**:
  1. `pickTransport` yields `{mcp,'mcp-live'}` when MCP probe within 2 s succeeds.
  2. Yields `{ssh,'mcp-unreachable'}` on MCP timeout with `mcp_endpoint` configured.
  3. Yields `{ssh,'mcp-not-configured'}` when `mcp_endpoint` is absent.
  4. `assembleRuntime` fails closed on invalid config (exit 11) and unreachable vault (exit 20).
  5. Runtime object exposes `config`, `resolver`, `pool`, `mcpDiscovery`, `audit`.
- **Lint/Test commands**: `npm run lint`, `npx jest tests/live`.
- **Effort**: 3 h base → **3.6 h buffered**.
- **Track**: B (Core wiring).
- **Risks**: `L` — `MCPDiscovery.probe` may not accept a timeout argument; mitigation: wrap call in `Promise.race`.

### TASK-007 — In-memory SSH key material in `ssh-client.ts` + factory (FR-CONN-02)

- **ID**: TASK-007 (≡ TDD T-07)
- **Description**: Modify `src/connection/ssh-client.ts` and `src/connection/factory.ts` to accept private-key bytes via `Buffer` (from `ResolvedSecret.value`) instead of only a filesystem path. On `Connection.close()`, zero the buffer.
- **Files**:
  - Modify: `src/connection/ssh-client.ts`, `src/connection/factory.ts`
  - Modify tests: `tests/connection/ssh-client.test.ts`, `tests/connection/factory.test.ts`
- **Dependencies**: TASK-005a (needs `ResolvedSecret`).
- **Acceptance criteria**:
  1. `ssh-client` unit tests pass with `privateKey: Buffer` input and no filesystem access to `~/.ssh` (verified with `fs.readFile` spy).
  2. `Connection.close()` calls `buf.fill(0)` on all held credential buffers (spy assertion).
  3. Backward-compat: existing factory callers within the repo compile without change (thanks to overload accepting either `Buffer` or path).
  4. No net writes on the `HOME` filesystem during a full `npx jest tests/connection` run (asserted by tmp-HOME test wrapper).
- **Lint/Test commands**: `npm run lint`, `npx jest tests/connection`.
- **Effort**: 3 h base → **3.6 h buffered**.
- **Track**: B (Core wiring).
- **Risks**: `M` — Underlying `ssh2` library requires `privateKey` as `Buffer` or `string`; mitigation: pass `Buffer` directly, no encoding conversion.

### TASK-008 — `homelab connect --test` CLI + audit wiring (FR-CONN-03/04)

- **ID**: TASK-008 (≡ TDD T-08)
- **Description**: Implement `src/cli/commands/connect.ts` (`runConnectTest`) per TDD §3.8, register under `homelab connect --test` in `src/cli/index.ts`, produce per-host result table (text + `--json`), audit each attempt via `connect.test` event including `credential_ref_hash`, `transport_reason`, `latency_ms`.
- **Files**:
  - Create: `src/cli/commands/connect.ts`
  - Modify: `src/cli/index.ts` (wire command; use `assembleRuntime`)
  - Create tests: `tests/cli/connect.test.ts`
- **Dependencies**: TASK-006, TASK-007.
- **Acceptance criteria**:
  1. All 3 declared hosts (gallifrey-lab-01/02, unraid.pwatson.space) surface in the output table.
  2. `--json` output matches audit schema exactly (verified against JSON schema fixture).
  3. Exit code 0 iff every host `ok`; else `EXIT_CONNECT_FAIL` (31 per §6).
  4. Errors redacted via redactor before being placed in the table's `error` column.
  5. No `Connection.exec*` call issued in the test path (spy assertion — read-only ping only).
- **Lint/Test commands**: `npm run lint`, `npx jest tests/cli/connect.test.ts`.
- **Effort**: 3 h base → **3.6 h buffered**.
- **Track**: B (Core wiring).
- **Risks**: `L` — `ConnectionPool.getConnection` behaviour may open a persistent pool; mitigation: use `close()` after `ping`.

### TASK-009 — Live observation probes for Swarm + Unraid (FR-OBS-*)

- **ID**: TASK-009 (≡ TDD T-09)
- **Description**: Implement `src/observation/live-probes.ts` (`buildLiveProbes`) wiring SPEC-002-1-02 Docker Swarm container health + SPEC-002-1-03 Unraid array/pool probes into existing `ObservationCollector`. Extend `homelab observe scan` to consume the config-driven host list.
- **Files**:
  - Create: `src/observation/live-probes.ts`
  - Modify: `src/cli/commands/observe.ts` (accept runtime, feed probes)
  - Create tests: `tests/observation/live-probes.test.ts`
- **Dependencies**: TASK-008.
- **Acceptance criteria**:
  1. `buildLiveProbes(config)` returns one Swarm probe per Swarm host and two probes (array + pool) per Unraid host.
  2. `homelab observe scan` persists observations for each host via existing `ObservationStore.persist()`.
  3. Every observation validates against SPEC-002-1-01 schema (Zod check in test).
  4. No mutations executed (verified via `MutationBarrier` reused from TASK-010 or a temporary read-only spy).
- **Lint/Test commands**: `npm run lint`, `npx jest tests/observation/live-probes.test.ts`.
- **Effort**: 3 h base → **3.6 h buffered**.
- **Track**: B (Core wiring).
- **Risks**: `M` — Existing Swarm/Unraid probes may assume config-free hostname discovery; mitigation: pass hostname explicitly.

### TASK-010 — Autofix CLI (propose / dry-run / abort-pending) + MutationBarrier (FR-SAF-*)

- **ID**: TASK-010 (≡ TDD T-10)
- **Description**: Implement `src/cli/commands/autofix.ts` (three sub-commands per TDD §3.10) and a `MutationBarrier` connection wrapper that throws on any non-read op during dry-run. `abort-pending` delegates to existing `cancelDelayedAction`.
- **Files**:
  - Create: `src/cli/commands/autofix.ts`, `src/safety/mutation-barrier.ts`
  - Modify: `src/cli/index.ts` (register subcommands)
  - Create tests: `tests/cli/autofix.test.ts`, `tests/safety/mutation-barrier.test.ts`
- **Dependencies**: TASK-008.
- **Acceptance criteria**:
  1. `homelab autofix propose <observation-id>` writes `.autonomous-dev/proposals/<id>.json` matching TDD §4.3 Proposal shape.
  2. `homelab autofix dry-run <proposal-id>` prints ladder classification + gate outcome (`WOULD_REQUIRE_TYPED_CONFIRM` | `WOULD_DELAY_24H` | `WOULD_EXECUTE_L2_PLUS`); never mutates.
  3. `MutationBarrier` throws on `exec`, `execCommand`, `writeFile`, `spawn` calls on wrapped `Connection`; unit-tested with a stub.
  4. `homelab autofix abort-pending <action-id>` writes `action.cancelled` audit event via `cancelDelayedAction`.
  5. CI guard test (`tests/safety/no-live-l0.test.ts`) asserts dry-run pathway invokes `MutationBarrier` on every proposal.
- **Lint/Test commands**: `npm run lint`, `npx jest tests/cli/autofix.test.ts tests/safety`.
- **Effort**: 4 h base → **4.8 h buffered**.
- **Track**: B (Core wiring).
- **Risks**: `M` — Proposal ID generation collision with existing action-id space; mitigation: prefix `prop-` (per TDD example).

### TASK-011 — `homelab config validate` + `homelab vault ping` CLI helpers (FR-CFG-03, FR-SEC-01)

- **ID**: TASK-011 (≡ TDD T-11)
- **Description**: Add two small CLI commands referenced by the runbook: `homelab config validate [--config <path>]` and `homelab vault ping`. Exit codes per TDD §6.
- **Files**:
  - Create: `src/cli/commands/config-validate.ts`, `src/cli/commands/vault-ping.ts`
  - Modify: `src/cli/index.ts`
  - Create tests: `tests/cli/config-validate.test.ts`, `tests/cli/vault-ping.test.ts`
- **Dependencies**: TASK-004, TASK-005a.
- **Acceptance criteria**:
  1. `config validate` returns exit 0 on the valid fixture, 11 on any invalid fixture.
  2. `vault ping` returns exit 0 on stub 200, exit 20 on connect refuse, exit 21 on auth failure.
  3. Output redacted (no vault path or token in error text beyond the whitelist).
- **Lint/Test commands**: `npm run lint`, `npx jest tests/cli/config-validate.test.ts tests/cli/vault-ping.test.ts`.
- **Effort**: 1 h base → **1.2 h buffered**.
- **Track**: B (Core wiring).
- **Risks**: none material.

### TASK-012a — Deploy: `install.sh` (FR-DEP-01, ADR-05)

- **ID**: TASK-012a (split of TDD T-12, part 1)
- **Description**: Implement `scripts/deploy/install.sh <artifact.tgz>` per TDD §3.12: extract to `~/.claude/plugins/autonomous-dev-homelab-<version>/`, `npm ci --omit=dev`, symlink `~/.claude/plugins/autonomous-dev-homelab`, append install manifest to `~/.autonomous-dev-homelab/installs.log`.
- **Files**:
  - Create: `scripts/deploy/install.sh`
  - Create tests: `tests/integration/deploy-install.test.ts` (temp `HOME` via `mkdtemp`)
- **Dependencies**: TASK-003.
- **Acceptance criteria**:
  1. Install a fixture tarball into temp `HOME`; symlink points at extracted dir; `installs.log` has one JSON line.
  2. Re-running against same version is idempotent (skip or replace, documented).
  3. POSIX-shell only (validated via `shellcheck`).
- **Lint/Test commands**: `shellcheck scripts/deploy/install.sh`, `npx jest tests/integration/deploy-install.test.ts`.
- **Effort**: 1.5 h base → **1.8 h buffered**.
- **Track**: C (Deploy).
- **Risks**: `L` — `shellcheck` may not be installed on CI; mitigation: fall back to `bash -n` syntax check.

### TASK-012b — Deploy: `upgrade.sh` + `rollback.sh` (FR-DEP-01, NFR-07)

- **ID**: TASK-012b (split of TDD T-12, part 2)
- **Description**: Implement `scripts/deploy/upgrade.sh` (install alongside, flip symlink, retain 5) and `scripts/deploy/rollback.sh` (flip to previous or specified version). Round-trip tested.
- **Files**:
  - Create: `scripts/deploy/upgrade.sh`, `scripts/deploy/rollback.sh`
  - Create tests: `tests/integration/deploy-upgrade-rollback.test.ts`
- **Dependencies**: TASK-012a.
- **Acceptance criteria**:
  1. Sequence install v0.2.0 → upgrade to v0.2.1 → rollback → symlink points at v0.2.0.
  2. `installs.log` shows 3 lines: install, upgrade, rollback.
  3. After 6 upgrades, only 5 most-recent versions retained on disk.
- **Lint/Test commands**: `shellcheck scripts/deploy/*.sh`, `npx jest tests/integration/deploy-upgrade-rollback.test.ts`.
- **Effort**: 1.5 h base → **1.8 h buffered**.
- **Track**: C (Deploy).
- **Risks**: `M` — Symlink flip race on macOS APFS vs Linux; mitigation: use `ln -sfn` (atomic replace).

### TASK-013 — Operator runbook (FR-DEP-02)

- **ID**: TASK-013 (≡ TDD T-13)
- **Description**: Author `docs/runbooks/REQ-000055-homelab-operator.md` with all 9 sections listed in TDD §3.13. Every command block copy-pastable; every error code in TDD §6 mentioned in Troubleshooting.
- **Files**:
  - Create: `docs/runbooks/REQ-000055-homelab-operator.md`
- **Dependencies**: TASK-011, TASK-012b (need `config validate`, `vault ping`, deploy scripts finalized to write accurate commands).
- **Acceptance criteria**:
  1. Runbook contains 9 sections in the order specified in TDD §3.13.
  2. Every command block resolves against actual CLI (spot-checked in TASK-014).
  3. Troubleshooting table includes exit codes 11, 12, 20, 21, 22, 23, 24, 30, 31, 40, 41, 50.
  4. Vault AppRole setup walkthrough included (env vars, permissions, kv path shape).
- **Lint/Test commands**: `npx markdownlint docs/runbooks/REQ-000055-homelab-operator.md` (if configured); manual review.
- **Effort**: 3 h base → **3.6 h buffered**.
- **Track**: D (Docs/Evidence).
- **Risks**: `L` — Runbook drift with CLI changes; mitigation: TASK-014 evidence step executes runbook commands verbatim.

### TASK-014 — Live smoke + evidence capture (FR-DEP-03, G-04, G-05)

- **ID**: TASK-014 (≡ TDD T-14)
- **Description**: Run the runbook end-to-end against Patrick's live homelab: discover → connect --test → observe scan → dry-run autofix (with `--abort-pending`) → portal panel screenshot. Capture the 10 evidence artifacts named in TDD §3.13.
- **Files**:
  - Create under `docs/evidence/REQ-000055/`:
    - `01-clean-build.log`, `02-cli-help.log`, `03-discover.log`, `04-inventory.yaml`,
    - `05-connect-test.log`, `06-audit-excerpt.log`, `07-observation-samples.json`,
    - `08-dry-run-proposal.json`, `09-portal-panel.png`, `10-sse-timing.md`
- **Dependencies**: TASK-009, TASK-010, TASK-011, TASK-013.
- **Acceptance criteria**:
  1. All 10 artifacts present, non-empty, and referenced by relative path in the runbook's Verification section.
  2. Audit excerpt contains `credential_ref_hash` values only (grep for known secret strings returns no matches).
  3. Portal screenshot shows populated inventory + at least one observation card.
  4. `10-sse-timing.md` documents ≤5 s SSE update latency (NFR-08).
- **Lint/Test commands**: `bash scripts/secret-scan.sh docs/evidence/REQ-000055/` (must exit 0).
- **Effort**: 3 h base → **3.6 h buffered**.
- **Track**: D (Docs/Evidence).
- **Risks**:
  - `H` — Live host unavailability; mitigation: retry per runbook, escalate as blocker before starting task.
  - `M` — Portal panel rendering issue → recorded as follow-up per PRD NG-01, not a blocker for THIS request unless it exposes a data-model regression.

### TASK-015 — Build pipeline integration tests (FR-TST-01, FR-BLD-02/03)

- **ID**: TASK-015 (new; splits FR-TST-01 verification from T-15)
- **Description**: Add `tests/integration/build-pipeline.test.ts` and `tests/integration/package.test.ts` that spawn `scripts/build.sh` and `scripts/package.sh` in a temporary directory and assert exit codes + artifact shape. Ensures FR-BLD-02/03/04 have executable regression guards.
- **Files**:
  - Create: `tests/integration/build-pipeline.test.ts`, `tests/integration/package.test.ts`
- **Dependencies**: TASK-002, TASK-003.
- **Acceptance criteria**:
  1. `build-pipeline.test.ts` runs `bash scripts/build.sh` (or a fast subset), asserts exit 0 and existence of `dist/cli/index.js`.
  2. `package.test.ts` runs `bash scripts/package.sh`, extracts the tarball, verifies file list.
  3. Both tests gated behind `INTEGRATION=1` env var to keep default jest run fast.
- **Lint/Test commands**: `INTEGRATION=1 npx jest tests/integration`.
- **Effort**: 2 h base → **2.4 h buffered**.
- **Track**: A (Build/Typecheck).
- **Risks**: `M` — Test time cost; mitigation: `INTEGRATION=1` gate + timeout of 180 s.

### TASK-016 — CI job wiring (FR-TST-01/02, NFR-01, NFR-09)

- **ID**: TASK-016 (≡ TDD T-15)
- **Description**: Add a CI script or GitHub Actions workflow running `npm ci` → `typecheck` → `lint` → `test` → `build:all`. Include the assertion that default `npm test` opens zero network connections toward gallifrey-lab-01/02 or unraid.pwatson.space (`LIVE=1` gates live tests only).
- **Files**:
  - Create: `.github/workflows/ci.yml` (or `scripts/ci.sh` if GH Actions not desired)
  - Create test: `tests/net/no-live-in-unit.test.ts` (uses `nock`/`undici` interceptor to fail if a real socket opens to disallowed hosts)
- **Dependencies**: TASK-002, TASK-015.
- **Acceptance criteria**:
  1. Sample PR (or dry-run act) runs pipeline green ≤5 minutes.
  2. `tests/net/no-live-in-unit.test.ts` fails when a fixture attempts a socket to `gallifrey-lab-01`.
  3. Workflow triggers on `pull_request` and `push` to main.
- **Lint/Test commands**: `act pull_request` (if `act` available) or GH Actions dry-run.
- **Effort**: 1 h base → **1.2 h buffered**.
- **Track**: A (Build/Typecheck).
- **Risks**: `L` — Runner network egress policy; mitigation: unit tests run locally with `nock` interceptor regardless of CI network state.

---

## 3. Dependency Graph

```
                         TASK-001 (TS2493 fix)
                            │
             ┌──────────────┼───────────────────────┐
             ▼              ▼                       ▼
       TASK-002        TASK-004 (config)      (feeds all downstream)
       (build.sh)          │
             │             ▼
             ▼          TASK-005a (vault client)
       TASK-003             │
       (package.sh)         ├──────────────────────┐
             │              ▼                      ▼
             │          TASK-005b            TASK-007 (ssh in-mem)
             │          (redactor)                  │
             │              │                      │
             │              ▼                      │
             │          TASK-006 ◀─────────────────┘
             │          (transport + bootstrap)
             │              │
             │              ▼
             │          TASK-008 (connect --test) ─┬─► TASK-009 (observe live)
             │              │                      │
             │              │                      └─► TASK-010 (autofix)
             │              │
             │              ▼
             │          TASK-011 (config validate + vault ping)
             │              │
       TASK-015 ◀───────────┤
       (integration tests)  │
             │              │
             ▼              ▼
       TASK-016        TASK-012a (install.sh)
       (CI)                 │
                            ▼
                       TASK-012b (upgrade/rollback)
                            │
                            ▼
                       TASK-013 (runbook)
                            │
                            ▼
                       TASK-014 (evidence + live smoke)
```

**Critical path** (longest sequential chain, 14 tasks, **30.6 h buffered**):

`TASK-001 → TASK-004 → TASK-005a → TASK-005b → TASK-006 → TASK-007 → TASK-008 → TASK-009 → TASK-010 → TASK-011 → TASK-012a → TASK-012b → TASK-013 → TASK-014`

Note: TASK-007 depends on TASK-005a (not 005b), so it can start as soon as TASK-005a finishes; the critical path above assumes strict sequencing on Track B for a single executor. With two executors, TASK-007 and TASK-005b run in parallel (see §4).

---

## 4. Parallel Execution Schedule

Four tracks; two-executor scenario shown. `t` = wall-clock hours from kickoff, buffered estimates.

```
Track A (Build/Typecheck)     Track B (Core wiring)        Track C (Deploy)      Track D (Docs/Evidence)
                              TASK-001 (0.6h)                                    
──────────────────────────── barrier ──────────────────────────
TASK-002 (2.4h)               TASK-004 (3.6h)               (waits on A→T-003)
TASK-003 (2.4h)               TASK-005a (3.6h)              
TASK-015 (2.4h)               TASK-005b (2.4h) ║ TASK-007   
TASK-016 (1.2h)                                (3.6h)       
                              TASK-006 (3.6h)               TASK-012a (1.8h)
                              TASK-008 (3.6h)               TASK-012b (1.8h)
                              TASK-009 (3.6h) ║ TASK-010    
                                                (4.8h)      
                              TASK-011 (1.2h)               
──────────────────────────── barrier ──────────────────────────
                                                                                 TASK-013 (3.6h)
                                                                                 TASK-014 (3.6h)
```

**Two-executor wall-clock estimate**: **~25 h** (Track B dominates critical path; Track A finishes ~8.4 h; Track C piggybacks after TASK-003 at ~5 h and finishes ~8.6 h; Track D starts at hour ~22).

**Single-executor wall-clock estimate**: **~47.4 h** (sum of buffered estimates).

**File-conflict analysis**:
- `src/cli/index.ts` is modified by TASK-002 (indirect via scripts only, no code), TASK-008, TASK-010, TASK-011. Sequence within Track B avoids conflict.
- `package.json` modified by TASK-002 (scripts) and possibly TASK-005a (deps if `undici` added). Merge order: TASK-002 → TASK-005a.
- No other cross-track file collisions.

---

## 5. Risk Register

| # | Risk | Severity | Track/Task | Mitigation |
|---|---|---|---|---|
| R-1 | Live homelab hosts unreachable during TASK-014 | H | D / T-014 | Pre-verify connectivity at start of task; escalate before build burn; runbook makes retry procedure explicit. |
| R-2 | Vault kv v1/v2 mount path ambiguity | M | B / T-005a | Config declares full path verbatim; add fixture per mount type. |
| R-3 | Existing `AuditWriter` lacks sink-filter hook | M | B / T-005b | Introduce private `WriteInterceptor` chain; behavior-equivalence test on non-secret events. |
| R-4 | `src/` leaking test-only imports blocks `tsconfig.build.json` | M | A / T-002 | Isolate leaks; move helpers into `tests/`; fail fast with clear message. |
| R-5 | `ssh2` API surface change with `Buffer` key material | M | B / T-007 | Pin `ssh2` version; test with real keypair fixture. |
| R-6 | Portal panel regression discovered during TASK-014 | M | D / T-014 | Per PRD NG-01, file follow-up request unless data-model regression exposed. |
| R-7 | Symlink flip race across macOS/Linux in deploy | M | C / T-012b | Use `ln -sfn`; test on both OSes if available. |
| R-8 | CI runner cold `npm ci` exceeds 120 s | L | A / T-002, T-016 | Exclude `npm ci` from NFR-01 timing; cache node_modules in CI. |
| R-9 | Proposal id collision with existing action-id space | M | B / T-010 | Prefix with `prop-`; add uniqueness test. |
| R-10 | Config schema too strict rejects valid YAML anchors | L | B / T-004 | Whitelist anchor semantics or reject with clear guidance. |
| R-11 | Runbook drifts from CLI as commands change | L | D / T-013 | TASK-014 executes runbook commands verbatim; any deviation forces runbook update. |
| R-12 | Missing named export `CancelAuditFn` from `cancel-action.ts` | L | A / T-001 | Add one-line export as part of TASK-001. |

No **high-severity** risks other than the live-host dependency (R-1). No circular dependencies detected in the graph. No blockers requiring TDD revision at this time.

---

## 6. TDD → Task Coverage

Every FR / ADR / TDD design section maps to at least one task:

| TDD element | Task(s) |
|---|---|
| §3.1 / FR-BLD-01 / ADR-01 | TASK-001 |
| §3.2 / FR-BLD-02/03 | TASK-002 |
| §3.3 / FR-BLD-04 / ADR-05 | TASK-003 |
| §3.4 / FR-CFG-01/02/03 | TASK-004, TASK-011 |
| §3.5 / FR-SEC-01..04 / ADR-02, ADR-03 | TASK-005a, TASK-005b |
| §3.6 / FR-CONN-01 / ADR-06 | TASK-006 |
| §3.7 / FR-DISC-01..03 | TASK-009 (piggybacks; extends `runDiscover`), TASK-014 (evidence) |
| §3.8 / FR-CONN-03/04, FR-CONN-02 | TASK-007, TASK-008 |
| §3.9 / FR-OBS-01..03 | TASK-009 |
| §3.10 / FR-SAF-01..04 / ADR-04 | TASK-010 |
| §3.11 / FR-UI-01..03 | TASK-014 (procedural evidence) |
| §3.12 / FR-DEP-01 | TASK-012a, TASK-012b |
| §3.13 / FR-DEP-02/03 | TASK-013, TASK-014 |
| §7 / FR-TST-01..03 | TASK-015, TASK-016 |
| §6 error taxonomy | TASK-005b (secret), TASK-004 (config), TASK-008 (connect), TASK-002 (build), TASK-013 (runbook cross-ref) |
| §8 Security | TASK-005b (redactor), TASK-002 (secret-scan), TASK-007 (buffer zero), TASK-014 (audit hash check) |
| §9 Performance NFR-01 | TASK-002, TASK-015 |
| §9 NFR-02/03 | TASK-006, TASK-008 |
| §9 NFR-08 | TASK-014 |

**Gap check**: none — every FR-* referenced in the PRD is present in the task table above.

---

## 7. Execution Notes for Downstream Agents

1. **Do not weaken the safety model.** TASK-010's `MutationBarrier` is a load-bearing guarantee; any executor tempted to bypass it must reopen this plan for review.
2. **Do not commit `homelab.config.yaml` with real hosts.** The example lives in the TDD and runbook only; the operator's actual config is stored under `$HOME`.
3. **Evidence artifacts must be redacted.** TASK-014's acceptance requires `bash scripts/secret-scan.sh docs/evidence/REQ-000055/` to exit 0.
4. **Preserve all ~697 existing tests green** at every task boundary. Any red at the end of a task → do not proceed to the next task; open a fix task first.
5. **All new tests default to offline.** Live tests are gated behind `LIVE=1`.

---

## 8. Blockers / Open Items

- None blocking plan execution. TDD open questions Q-01..Q-07 and TQ-01..TQ-03 all have working assumptions; deviations should surface as new requests, not in-flight scope changes to REQ-000055.

---

*Plan complete. Ready for plan-review.*
